export {
  PlayerManager,
  getPlayerManager,
  resetPlayerManager,
  PLAYER_OUTPUT_SIZE,
} from './PlayerManager';

export {
  type PlayerState,
  type PlayerSettings,
  type PlayerDimensions,
  type PlayerColors,
  DEFAULT_PLAYER_STATE,
  DEFAULT_PLAYER_SETTINGS,
  DEFAULT_PLAYER_DIMENSIONS,
  DEFAULT_PLAYER_COLORS,
  BASE_PLAYER_DIMENSIONS,
  computeScaledDimensions,
  DAMAGE_FLAGS,
} from '../types/PlayerConfig';
